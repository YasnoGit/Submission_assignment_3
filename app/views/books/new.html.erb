<h2>New book</h2>
  <%= form_with model: @book do |f| %>
    <label>Title</label>
      <%= f.text_field :book_title %>
    <label>Opinion</label>
      <%= f.text_area :book_body %>
    <%= f.submit 'Create Book' %>
  <% end %>